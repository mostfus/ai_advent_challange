[project]
name = "day7-save-context"
version = "0.1.0"
description = "An LLM agent whose conversation survives a restart, with a web GUI on top"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.141.1",
    "httpx>=0.28.1",
    "python-dotenv==1.2.3",
    "uvicorn[standard]>=0.52.4",
]
