{
  "name": "day11-ui-tests",
  "private": true,
  "description": "Headless jsdom checks for the agent chat UI, its memory layers, its context strategies, its branches and its token readout",
  "scripts": {
    "test": "node tests/ui-test.js"
  },
  "devDependencies": {
    "jsdom": "^27.0.0"
  }
}
