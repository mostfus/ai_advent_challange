[project]
name = "day6-simple-agent"
version = "0.1.0"
description = "A simple LLM agent with its own HTTP client, and a web GUI on top"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.141.1",
    "httpx>=0.28.1",
    "python-dotenv==1.2.3",
    "uvicorn[standard]>=0.52.4",
]
